# Selenium Automation Bot (Linux)

This is a web automation bot built using Selenium.  
It uses `webdriver-manager` so **no manual ChromeDriver installation is needed**.

## 🚀 Run Instructions

```bash
pip install -r requirements.txt
chmod +x run.sh
./run.sh
